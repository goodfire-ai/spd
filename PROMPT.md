Please first ultrathink and make a plan for your implementation in a new file that you create called PLAN.md. If the file exists, overwrite it.

Each time you make a change, run the tests (make test) and type checker (make type), and update the PLAN.md file with the changes you made.

Reassess the plan after each change.

Once you're finished, run the formatter (make format).